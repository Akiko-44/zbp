<li>
		<h3><%= data.name %></h3>
		<% if(data.children && data.children.length > 0){ %>
		<div>
			<% for(var i = 0; i < data.children.length; i++){ %>
				<a class="link" href="../../gallery/list/<%=data.children[i].id%>"><%= data.children[i].name %></a>
			<% } %>
		</div>
		<% } %>
</li>
